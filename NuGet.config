<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <config>
    <!-- Store packages inside the repo to avoid using system-level fallback folders -->
    <add key="globalPackagesFolder" value=".nuget/packages" />
  </config>
</configuration>